; Upload limits — must match or exceed Laravel's file|max:51200 validation rule
upload_max_filesize = 50M
post_max_size       = 55M

; Memory and execution
memory_limit        = 256M
max_execution_time  = 120
